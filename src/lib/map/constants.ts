import type { LayoutOptions } from "./layout";

/** Map coordinate space. The path column is rendered at this width (it only stretches slightly on phones). */
export const MAP_LAYOUT: LayoutOptions = {
  width: 420,
  margin: 72,
  headerHeight: 170,
  levelSpacing: 150,
  minBodyHeight: 330,
};

export const SCENERY_SPACING = 180;
